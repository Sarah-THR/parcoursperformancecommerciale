@page "/history/{userId}/{weekId}"
@using EcoleDeLaPerformance.Ui.Shared.Components

<UserDetails userId="@userId" weekId="@weekId" />

@code {
    [Parameter]
    public string userId { get; set; }

    [Parameter]
    public string weekId { get; set; }
}
